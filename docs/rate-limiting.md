# Rate Limiting

O provedor em memoria e somente para desenvolvimento local e testes.

Ambientes serverless exigem provedor externo atras da abstracao existente. A opcao prevista e Upstash Redis REST via HTTPS, configurada por variaveis de ambiente server-side.

Nao versionar tokens, salts reais ou URLs de servico privadas.
